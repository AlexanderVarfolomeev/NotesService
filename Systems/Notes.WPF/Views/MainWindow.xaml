<Window x:Class="Notes.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:com="clr-namespace:Notes.WPF.Infrastructure.Commands"
        xmlns:vm="clr-namespace:Notes.WPF.ViewModels"
        xmlns:ttmodel="clr-namespace:Notes.WPF.Models.TaskTypes"
        xmlns:local="clr-namespace:Notes.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Обновить данные" Command="{Binding RefreshTaskTypesDataCommand}"/>
            <MenuItem Header="Выход" Margin="10, 0"/>
        </Menu>
        <TabControl DockPanel.Dock="Top">
            <TabItem Header="Закладка 1"></TabItem>
            <TabItem Header="Закладка 2" >
                <TabItem.InputBindings>
                    <MouseBinding Command="{Binding RefreshTaskTypesDataCommand}" MouseAction="RightClick"/>
                </TabItem.InputBindings>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"></RowDefinition>
                        <RowDefinition Height="5*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid Grid.Column="0" Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Grid.Row="0" Content="Добавить тип."  Command="{Binding AddTaskTypeCommand }"></Button>
                        <Button Grid.Column="1" Grid.Row="0" Content="Удалить тип." Command="{Binding DeleteTaskTypeCommand}" CommandParameter="{Binding SelectedType}"></Button>
                    </Grid>
                    <DataGrid ItemsSource="{Binding TaskTypes}" SelectedItem="{Binding SelectedType}" AutoGenerateColumns="False" Grid.Column="0" Grid.Row="1">
                        <DataGrid.ItemTemplate>
                            <DataTemplate DataType="ttmodel:TaskType">
                            </DataTemplate>
                        </DataGrid.ItemTemplate>
                        <DataGrid.Columns>
                            
                            <DataGridTextColumn Header="Имя" Binding="{Binding Path=Name}" Width="*"></DataGridTextColumn>
                            <DataGridTextColumn Header="Цвет" Binding="{Binding Path=Color.Name}" Width="1*"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Закладка 3"></TabItem>
        </TabControl>
    </DockPanel>
</Window>
